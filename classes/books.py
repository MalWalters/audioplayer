#This is the class file for audio player

class Book:
	""" Base Book class"""
	def __init__(self, id, title, url, playtime):
		self.id = id
		self.title = title
		self.url = url
		self.playtime = playtime

	def addPlaytime(currentPlaytime):
		self.playTime = currentPlayTime
